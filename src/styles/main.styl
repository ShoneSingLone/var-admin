body {
    background: #222;
    font-size: 1.1rem;
    font-family: Arial, Helvetica, sans-serif;

    div {
        margin: 20px;
        font-size: 2em;
        text-align: center;
        color: #fff;

        & > img {
            width: 300px;
            height: 300px;
            display: block;
            margin: 20px auto;
        }
    }
}